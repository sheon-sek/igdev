<!-- Generated by cmd/igdev-docs from the CLI's command definitions. Do not edit: run `make reference` instead. -->

# `igdev catalog status`

status reports both layers of the Effective Catalog: where each came from, its
sha256 digest, and its row counts.

The digest is the layer's identity and the review surface. The core digest covers
the embedded plane files in a frozen order; the overlay digest covers the declared
overlay files, in declaration order, so any edit to a tracked overlay changes it.
Two checkouts that resolve the same capabilities therefore report the same
digests.

Row counts are reported because a human wants to see the shape of the knowledge,
never asserted: a count is a maintenance chore, a digest is a proof.

The Ignition version resolved comes from the config precedence chain, so
--config ignition.version=8.1.21 asks about a version this binary carries no
catalog for and fails with IGDEV_E_CATALOG_VERSION_MISSING.

## Usage

```text
igdev catalog status [flags]
```

## Examples

```text
  igdev catalog status
  igdev catalog status --json
```

## Options

```text
  -h, --help   help for status
```

## Global options

```text
      --config stringArray   override one config key for this run, key=value (highest precedence)
      --json                 emit the JSON envelope on stdout (machine mode)
```

## Agent usage

pass --json for the machine contract. data carries ignition_version, core
(source, digest, counts), overlay (source, digest, paths, counts), and effective (the
layer sum). Compare digests across checkouts to prove the same capabilities resolve; a
version this binary carries no catalog for fails with IGDEV_E_CATALOG_VERSION_MISSING.
