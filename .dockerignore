# Keep the Docker build context intentionally minimal.
# The build context is the repository root only so Docker can access generated
# module staging under .runtime/docker/modules/.

**
!docker/
!docker/ignition/
!docker/ignition/Dockerfile
!.runtime/
!.runtime/docker/
!.runtime/docker/modules/
!.runtime/docker/modules/**
