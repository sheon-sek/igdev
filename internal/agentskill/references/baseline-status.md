<!-- Generated by cmd/igdev-docs from the CLI's command definitions. Do not edit: run `make reference` instead. -->

# `igdev baseline status`

status reports the staged Baseline as JSON: whether one is staged, where it is,
how large it is, the source, digest, and timestamp its record carries, and the
restore arguments the next fresh Gateway launch applies. Without a staged file it
reports staged false and nothing else.

The staged file is the state and the record is only a note about it, so a record
that is missing, unreadable, or no longer describes the staged file (a different
size) leaves the Baseline staged without provenance — a hand-placed restore.gwbk
still counts.

## Usage

```text
igdev baseline status [flags]
```

## Examples

```text
  igdev baseline status
  igdev baseline status --json
```

## Options

```text
  -h, --help   help for status
```

## Global options

```text
      --config stringArray   override one config key for this run, key=value (highest precedence)
      --json                 emit the JSON envelope on stdout (machine mode)
```

## Agent usage

pass --json for the machine contract. data carries staged, and — when a
file is staged — path, bytes, source, sha256, staged_at, and restore_args. staged true
without provenance fields (a hand-placed restore.gwbk) is deliberate: the file is the
state and the record is a note.
