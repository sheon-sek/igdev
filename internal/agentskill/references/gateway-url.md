<!-- Generated by cmd/igdev-docs from the CLI's command definitions. Do not edit: run `make reference` instead. -->

# `igdev gateway url`

url prints the Gateway URL recorded in the Checkout Setup: the loopback address and
the port allocated to this Instance. Nothing in igdev assumes 8088 (ADR 0003), so
this is the only URL to script against.

## Usage

```text
igdev gateway url [flags]
```

## Examples

```text
  igdev gateway url
  curl "$(igdev gateway url)/"
```

## Options

```text
  -h, --help   help for url
```

## Global options

```text
      --config stringArray   override one config key for this run, key=value (highest precedence)
      --json                 emit the JSON envelope on stdout (machine mode)
```

## Agent usage

pass --json for the machine contract. data is the address block, and
data.url is the only URL to script against: the port came from the Checkout Setup, so
nothing in igdev or its callers may assume 8088.
