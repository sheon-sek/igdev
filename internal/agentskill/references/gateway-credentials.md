<!-- Generated by cmd/igdev-docs from the CLI's command definitions. Do not edit: run `make reference` instead. -->

# `igdev gateway credentials`

credentials reports the Gateway admin username and the source of the password.

The password itself is printed only with --json, so it never lands in a terminal's
scrollback, a shell history, or a CI log: this is the one machine-readable way to
obtain it. The value comes from IGDEV_GATEWAY_ADMIN_PASSWORD when that is set, and
from the 0600 .igdev/local.toml setup wrote otherwise.

## Usage

```text
igdev gateway credentials [flags]
```

## Examples

```text
  igdev gateway credentials --json
  curl -u "$(igdev gateway credentials --json | jq -r '.data.username'):..." http://.../
```

## Options

```text
  -h, --help   help for credentials
```

## Global options

```text
      --config stringArray   override one config key for this run, key=value (highest precedence)
      --json                 emit the JSON envelope on stdout (machine mode)
```

## Agent usage

pass --json for the machine contract, and --json is the only dialect that
prints the password: human mode reports the username and where the password came from.
data carries username and password. A missing password (IGDEV_E_CONFIG_INVALID) is
repaired with igdev setup; IGDEV_GATEWAY_ADMIN_PASSWORD overrides both tiers for CI.
Never log the envelope: it is a secret.
